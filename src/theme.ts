import { createTheme } from '@mui/material/styles';

const retroTheme = createTheme({
	typography: {
		bigheading: {
			fontSize: 64,
		},
	},
	gradients: {
		superDark: '',
	},
});

export default retroTheme;
